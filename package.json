{
  "name": "youtube-class-recorder",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend for YouTube Class Recorder with Vercel backend functions.",
  "scripts": {
    "dev": "echo \"Run your frontend development server here\"",
    "build": "mkdir -p dist && cp index.html index.tsx metadata.json types.ts dist/ && cp -r components hooks services dist/"
  },
  "dependencies": {
    "@google/genai": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
}